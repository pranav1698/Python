# You want to combine many small strings together into a larger string.

parts = ['Is', 'Chicago', 'Not', 'Chicago?']
print ' '.join(parts)
# Is Chicago Not Chicago?

""" The + operator to joina a lot of strings is grossly ineffecient due to memory copies and garbage collection that occurs, """

print(a, b , c, sep=':')

